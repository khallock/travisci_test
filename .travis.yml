language: generic

os: osx
osx_image: xcode6.4

before_install:
  - brew cask install xquartz
  - wget -O "$(brew --prefix)/Library/Taps/caskroom/homebrew-cask/Casks/ncar-ncl.rb" https://raw.githubusercontent.com/khallock/homebrew-cask/fix_ncar-ncl/Casks/ncar-ncl.rb
  - brew cask install ncar-ncl

script:
  - ncl -V
